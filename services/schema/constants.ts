export enum AccountStatus {
  Active = 'Y',
  Inactive = 'N',
}

export enum ProductSaleStatus {
  Available = 1001,
  OutOfStock = 1002,
}

export enum OrderStatus {
  Pending = 1001,
  Completed = 1002,
  Cancelled = 1003,
}
